import 'package:flutter/material.dart';

class HomePosition extends StatelessWidget {
  const HomePosition({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('定位'),);
  }
}
